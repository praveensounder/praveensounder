name: Metrics
on:
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
     - name: default deploy
       uses: praveensounder/praveensounder@master
       with:
          aws_access_key_id: AKIA5G3MBGQF7GNPSVOO
          aws_secret_access_key: 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
          aws_region: us-east-1
          function_name: sheet
